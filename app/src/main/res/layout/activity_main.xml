<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
    android:id="@+id/tef"
    android:background="#333333"
    android:weightSum="1">


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Intentions"
        android:id="@+id/btnIntentions"
        style="@android:style/Theme.Black"
        android:layout_weight=".5"
        android:background="#555555"
        android:layout_below="@+id/btnPrayerRequest"
        android:layout_marginTop="20dp"
        android:layout_alignRight="@+id/btnPrayerRequest"
        android:layout_alignEnd="@+id/btnPrayerRequest"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:textColor="#DDDDDD" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Prayer Request"
        android:id="@+id/btnPrayerRequest"
        style="@android:style/Theme.Black"
        android:layout_weight=".5"
        android:background="#555555"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:padding="10dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:textColor="#DDDDDD" />

    <WebView
        android:layout_width="wrap_content"
        android:layout_height="200dp"
        android:visibility="invisible"
        android:id="@+id/webView"
        android:layout_weight=".5"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignRight="@+id/btnPrayerRequest"
        android:layout_alignEnd="@+id/btnPrayerRequest"
        android:clickable="false"
        android:layout_below="@+id/btnIntentions"
        android:layout_marginTop="15dp"
        android:background="#333333" />


</RelativeLayout>
